import { Topic } from "./pythonLessons";

export const kotlinTopics: Topic[] = [
  {
    id: "kt-basics",
    title: "Kotlin Basics",
    icon: "🎯",
    color: "hsl(var(--syntax-type))",
    lessons: [
      {
        id: "kt-hello",
        title: "Hello World & Basics",
        description: "Your first Kotlin program.",
        content: `Kotlin is a modern, concise language that runs on the JVM. It's fully interoperable with Java.\n\nKotlin doesn't need semicolons, has type inference, and \`fun\` declares functions.\n\nUse \`val\` for immutable variables and \`var\` for mutable ones.`,
        code: "fun main() {\n    println(\"Hello, World!\")\n    \n    // val = immutable, var = mutable\n    val name = \"Alice\"\n    var age = 25\n    \n    println(\"Name: $name\")\n    println(\"Age: $age\")\n    \n    // String templates\n    println(\"$name is ${age + 1} next year\")\n    \n    // Type inference\n    val x = 42          // Int\n    val pi = 3.14       // Double\n    val active = true   // Boolean\n    \n    println(\"x=$x, pi=$pi, active=$active\")\n    \n    // Explicit types\n    val count: Int = 100\n    val message: String = \"Hello\"\n    println(\"$message, count=$count\")\n}",
        language: "Kotlin",
        expectedOutput: `Hello, World!\nName: Alice\nAge: 25\nAlice is 26 next year\nx=42, pi=3.14, active=true\nHello, count=100`,
        tips: [
          "Use val by default, var only when needed",
          "String templates: $var or ${expression}",
          "No semicolons needed",
          "Kotlin is null-safe by default",
        ],
        questions: [
          { question: "What does val mean?", options: ["Mutable variable", "Immutable variable", "A function", "A type"], answer: 1, explanation: "val declares an immutable (read-only) variable." },
          { question: "How do you interpolate in strings?", options: ["f'...'", "${}", "$var or ${expr}", "%s"], answer: 2, explanation: "Kotlin uses $ for simple variables and ${} for expressions." },
          { question: "Does Kotlin need semicolons?", options: ["Yes", "No", "Optional", "Only in loops"], answer: 1, explanation: "Kotlin does not require semicolons at the end of statements." },
          { question: "What does fun keyword do?", options: ["Creates a class", "Declares a function", "Makes code fun", "Creates a variable"], answer: 1, explanation: "fun is Kotlin's keyword for declaring functions." },
          { question: "Is Kotlin compatible with Java?", options: ["Yes, fully interoperable", "No", "Only one way", "Only for Android"], answer: 0, explanation: "Kotlin is fully interoperable with Java — you can call Java from Kotlin and vice versa." },
        ],
      },
      {
        id: "kt-null-safety",
        title: "Null Safety",
        description: "Kotlin's type system prevents null errors.",
        content: `Kotlin's type system distinguishes **nullable** (\`Type?\`) from **non-nullable** (\`Type\`) types.\n\nSafe call operator \`?.\` returns null if the object is null. Elvis operator \`?:\` provides a default.\n\n\`!!\` (not-null assertion) throws if null — use sparingly. \`let\` is useful for null checks.`,
        code: "fun main() {\n    // Nullable types\n    var name: String? = \"Alice\"\n    println(name?.length)      // 5\n    \n    name = null\n    println(name?.length)      // null\n    \n    // Elvis operator\n    val len = name?.length ?: 0\n    println(\"Length: $len\")    // 0\n    \n    // Safe call chain\n    val upper = name?.uppercase() ?: \"DEFAULT\"\n    println(upper)\n    \n    // let for null checks\n    val value: String? = \"Hello\"\n    value?.let {\n        println(\"Not null: $it, length: ${it.length}\")\n    }\n    \n    // Smart cast\n    val any: Any = \"Hello Kotlin\"\n    if (any is String) {\n        println(\"String length: ${any.length}\") // smart cast\n    }\n}",
        language: "Kotlin",
        expectedOutput: `5\nnull\nLength: 0\nDEFAULT\nNot null: Hello, length: 5\nString length: 12`,
        tips: [
          "Prefer ?. and ?: over !! assertions",
          "Kotlin eliminates NullPointerException at compile time",
          "let {} runs only if value is non-null",
          "Smart casts automatically cast after type checks",
        ],
        questions: [
          { question: "What does String? mean?", options: ["Optional string", "Nullable string", "Empty string", "String pointer"], answer: 1, explanation: "The ? suffix makes a type nullable — it can hold null." },
          { question: "What does ?. do?", options: ["Throws on null", "Returns null if object is null", "Checks type", "Creates nullable"], answer: 1, explanation: "Safe call ?. returns null instead of throwing if the receiver is null." },
          { question: "What does ?: do?", options: ["Checks null", "Provides default if null (Elvis)", "Asserts non-null", "Creates nullable"], answer: 1, explanation: "The Elvis operator provides a fallback value when the left side is null." },
          { question: "What does !! do?", options: ["Checks null safely", "Asserts non-null (throws if null)", "Converts to nullable", "Nothing"], answer: 1, explanation: "!! asserts that a value is non-null — throws NPE if it is null." },
          { question: "What is a smart cast?", options: ["Explicit casting", "Auto-cast after type check", "Unsafe cast", "Dynamic typing"], answer: 1, explanation: "After an 'is' check, Kotlin automatically casts the variable." },
        ],
      },
      {
        id: "kt-functions",
        title: "Functions & Lambdas",
        description: "Functions, lambdas, and higher-order functions.",
        content: `Kotlin functions use \`fun\` keyword. They support **default parameters**, **named arguments**, **single-expression** bodies, and **extension functions**.\n\n**Lambdas** are anonymous functions: \`{ params -> body }\`. The last lambda argument can be placed outside parentheses.\n\n**Higher-order functions** take or return functions.`,
        code: `// Default and named parameters\nfun greet(name: String, greeting: String = "Hello") = "$greeting, $name!"\n\nprintln(greet("Alice"))\nprintln(greet("Bob", greeting = "Hi"))\n\n// Lambda\nval nums = listOf(1, 2, 3, 4, 5)\nval doubled = nums.map { it * 2 }\nprintln(doubled)\n\nval evens = nums.filter { it % 2 == 0 }\nprintln(evens)\n\nval sum = nums.reduce { acc, n -> acc + n }\nprintln("Sum: $sum")\n\n// Extension function\nfun String.isPalindrome(): Boolean {\n    val clean = this.lowercase().filter { it.isLetterOrDigit() }\n    return clean == clean.reversed()\n}\nprintln("racecar".isPalindrome())  // true\nprintln("hello".isPalindrome())    // false\n\n// Higher-order function\nfun operate(a: Int, b: Int, op: (Int, Int) -> Int): Int = op(a, b)\nprintln(operate(10, 3) { a, b -> a + b })  // 13`,
        language: "Kotlin",
        expectedOutput: `Hello, Alice!\nHi, Bob!\n[2, 4, 6, 8, 10]\n[2, 4]\nSum: 15\ntrue\nfalse\n13`,
        tips: [
          "Single-expression functions: fun f(x: Int) = x * 2",
          "'it' is the implicit parameter name for single-param lambdas",
          "Extension functions add methods to existing classes",
          "Trailing lambda syntax: list.map { ... }",
        ],
        questions: [
          { question: "What does 'it' refer to in lambdas?", options: ["The list", "The single parameter", "The index", "The return value"], answer: 1, explanation: "'it' is the implicit name for a single lambda parameter." },
          { question: "What are extension functions?", options: ["Inherited methods", "Functions added to existing types", "Abstract methods", "Static methods"], answer: 1, explanation: "Extension functions add new methods to existing classes without modifying them." },
          { question: "What is a higher-order function?", options: ["A recursive function", "A function that takes/returns functions", "A fast function", "An async function"], answer: 1, explanation: "Higher-order functions accept functions as parameters or return them." },
          { question: "What does trailing lambda syntax mean?", options: ["Lambda at start", "Last lambda outside parentheses", "Lambda in variable", "No lambda"], answer: 1, explanation: "If the last parameter is a lambda, it can be placed outside ()." },
          { question: "Can functions have default parameters?", options: ["Yes", "No", "Only in classes", "Only val params"], answer: 0, explanation: "Kotlin functions support default parameter values." },
        ],
      },
    ],
  },
  {
    id: "kt-collections",
    title: "Collections & Data Classes",
    icon: "📋",
    color: "hsl(var(--syntax-string))",
    lessons: [
      {
        id: "kt-collections",
        title: "Collections",
        description: "Lists, maps, sets, and collection operations.",
        content: `Kotlin has **immutable** (\`listOf\`, \`mapOf\`, \`setOf\`) and **mutable** (\`mutableListOf\`, etc.) collections.\n\nPowerful operations: \`map\`, \`filter\`, \`groupBy\`, \`partition\`, \`flatMap\`, \`associate\`, \`zip\`, \`chunked\`, \`windowed\`.`,
        code: `fun main() {\n    // Immutable list\n    val fruits = listOf("apple", "banana", "cherry", "date")\n    println(fruits.first())\n    println(fruits.last())\n    \n    // Map operations\n    val lengths = fruits.map { it.length }\n    println("Lengths: $lengths")\n    \n    val long = fruits.filter { it.length > 5 }\n    println("Long: $long")\n    \n    // Grouping\n    val grouped = fruits.groupBy { it.first() }\n    println("Grouped: $grouped")\n    \n    // Map\n    val ages = mapOf("Alice" to 25, "Bob" to 30)\n    println(ages["Alice"])\n    \n    // Mutable\n    val items = mutableListOf(1, 2, 3)\n    items.add(4)\n    items.removeAt(0)\n    println("Mutable: $items")\n    \n    // Destructuring\n    val (first, second) = listOf("a", "b", "c")\n    println("$first, $second")\n}`,
        language: "Kotlin",
        expectedOutput: `apple\ndate\nLengths: [5, 6, 6, 4]\nLong: [banana, cherry]\nGrouped: {a=[apple], b=[banana], c=[cherry], d=[date]}\n25\nMutable: [2, 3, 4]\na, b`,
        tips: [
          "Prefer immutable collections by default",
          "Use 'to' infix for creating Pairs: \"key\" to \"value\"",
          "Destructuring works with data classes and collections",
          "Collection operations are lazy with .asSequence()",
        ],
        questions: [
          { question: "What does listOf create?", options: ["Mutable list", "Immutable list", "Array", "Set"], answer: 1, explanation: "listOf creates a read-only (immutable) list." },
          { question: "How do you create a mutable list?", options: ["listOf()", "mutableListOf()", "arrayOf()", "List()"], answer: 1, explanation: "mutableListOf() creates a list you can modify." },
          { question: "What does 'to' infix do?", options: ["Casts type", "Creates a Pair", "Converts list", "Maps values"], answer: 1, explanation: "The 'to' infix function creates a Pair, commonly used with mapOf." },
          { question: "What does groupBy do?", options: ["Sorts", "Groups elements by key into a map", "Filters", "Flattens"], answer: 1, explanation: "groupBy groups elements by a key function into a Map<K, List<V>>." },
          { question: "What makes sequences different from lists?", options: ["They're mutable", "They evaluate lazily", "They're faster always", "They use less memory always"], answer: 1, explanation: "Sequences evaluate operations lazily, processing elements one at a time." },
        ],
      },
      {
        id: "kt-data-classes",
        title: "Data Classes & Sealed Classes",
        description: "Concise data modeling in Kotlin.",
        content: `**Data classes** auto-generate \`equals()\`, \`hashCode()\`, \`toString()\`, \`copy()\`, and \`componentN()\` functions.\n\n**Sealed classes** restrict inheritance to a closed set of subclasses — perfect for state modeling and \`when\` expressions.`,
        code: "// Data class\ndata class User(val name: String, val age: Int)\n\nval user = User(\"Alice\", 25)\nprintln(user)                    // auto toString\nprintln(user.copy(age = 26))     // copy with changes\n\nval (name, age) = user           // destructuring\nprintln(\"$name is $age\")\n\n// Sealed class\nsealed class Result {\n    data class Success(val data: String) : Result()\n    data class Error(val message: String) : Result()\n    object Loading : Result()\n}\n\nfun handle(result: Result): String = when (result) {\n    is Result.Success -> \"Got: ${result.data}\"\n    is Result.Error -> \"Error: ${result.message}\"\n    Result.Loading -> \"Loading...\"\n}\n\nprintln(handle(Result.Success(\"Hello\")))\nprintln(handle(Result.Error(\"Not found\")))\nprintln(handle(Result.Loading))",
        language: "Kotlin",
        expectedOutput: `User(name=Alice, age=25)\nUser(name=Alice, age=26)\nAlice is 25\nGot: Hello\nError: Not found\nLoading...`,
        tips: [
          "Data classes need at least one val/var in constructor",
          "copy() creates a new instance with optional changes",
          "Sealed classes make when expressions exhaustive",
          "Use object for singleton subclasses of sealed classes",
        ],
        questions: [
          { question: "What does data class auto-generate?", options: ["Only toString", "equals, hashCode, toString, copy", "Just getters", "Nothing extra"], answer: 1, explanation: "Data classes generate equals(), hashCode(), toString(), copy(), and componentN()." },
          { question: "What does copy() do?", options: ["Deep copy", "Creates new instance with optional changes", "Clones reference", "Copies to clipboard"], answer: 1, explanation: "copy() creates a new instance, optionally changing some properties." },
          { question: "What are sealed classes for?", options: ["Performance", "Restricted inheritance hierarchies", "Encryption", "Database models"], answer: 1, explanation: "Sealed classes restrict subclasses to a known set, enabling exhaustive when." },
          { question: "Why are sealed classes useful with when?", options: ["They're faster", "Compiler ensures all cases are handled", "They're simpler", "They use less memory"], answer: 1, explanation: "The compiler can verify that all subclasses are handled in when expressions." },
          { question: "What does object keyword create?", options: ["A class", "A singleton", "A function", "A variable"], answer: 1, explanation: "object creates a singleton — a class with exactly one instance." },
        ],
      },
    ],
  },
];
